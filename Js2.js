// Datatypes and ECMA Standards

const playerName = "Zubair"
let score = 0
let level = 1
let isPlaying = true
let lastGame = null

let newScore = score + 25
let newLevel = level + 1

console.log(playerName);
console.log(score);
console.log(isPlaying);
console.log(lastGame);

console.log(playerName, "newScore:", newScore, "newLevel:", newLevel);
